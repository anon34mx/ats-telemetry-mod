CREATE OR REPLACE TABLE viajes(
    -- id big int unsigned auto_increment,

    velocidad float(5,2),
    velocidadLimite smallint(3) unsigned,
    inserted_date datetime DEFAULT CURRENT_TIMESTAMP

    job_income
);